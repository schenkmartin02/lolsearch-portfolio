import { RoundingIntPipe } from './rounding-int.pipe';

describe('RoundingIntPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundingIntPipe();
    expect(pipe).toBeTruthy();
  });
});
