import { ChampionIdToRolePipe } from './champion-id-to-role.pipe';

describe('ChampionIdToRolePipe', () => {
  it('create an instance', () => {
    const pipe = new ChampionIdToRolePipe();
    expect(pipe).toBeTruthy();
  });
});
