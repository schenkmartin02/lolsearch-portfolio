import { ItemNotFoundPipe } from './item-not-found.pipe';

describe('ItemNotFoundPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemNotFoundPipe();
    expect(pipe).toBeTruthy();
  });
});
