import { ChampionIconFixPipe } from './champion-icon-fix.pipe';

describe('ChampionIconFixPipe', () => {
  it('create an instance', () => {
    const pipe = new ChampionIconFixPipe();
    expect(pipe).toBeTruthy();
  });
});
