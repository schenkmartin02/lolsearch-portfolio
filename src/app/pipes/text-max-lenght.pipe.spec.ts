import { TextMaxLenghtPipe } from './text-max-lenght.pipe';

describe('TextMaxLenghtPipe', () => {
  it('create an instance', () => {
    const pipe = new TextMaxLenghtPipe();
    expect(pipe).toBeTruthy();
  });
});
