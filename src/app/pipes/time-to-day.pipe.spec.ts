import { TimeToDayPipe } from './time-to-day.pipe';

describe('TimeToDayPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeToDayPipe();
    expect(pipe).toBeTruthy();
  });
});
